import { createSlice } from "@reduxjs/toolkit"



const initialState = {

}

const reducerSlice = createSlice({
    name: "user",
    initialState,
    reducers:{

    }
})

export default reducerSlice;